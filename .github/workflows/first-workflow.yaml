name: first-workflow
on:
  push:
    branches: [master]
  pull_request:
    branches: [master]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Installing dependencies
        run: npm install
      - name: Build
        run: npm run build
